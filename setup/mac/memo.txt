## 新規PC時作業
- .ssh移行
- dotfiles移行 # TODO: dotfile どこかにcommitしておくK
  - .bashrc / .bash_profile / .onelogin-aws.config
  - .gitconfig / .gitignore / .git_commit_template
  - .aws / .config (gcloud) / .kube / .vimrc
  - .saml2aws
- spotlightの検索範囲を減らす

## source codes
- git clone
- git secrets --install
- Unity Assetsなど移行

## memo
参考にできそう
https://github.com/paulmillr/dotfiles/blob/master/etc/macos-settings.sh
